import { TFormElement } from '@/types/FormType';

const FormInput = (props: TFormElement) => {
	return (
		<div className="form-body">
			<label className="form-body__label" htmlFor=""></label>
		</div>
	);
};

export default FormInput;
